# Final Outputs

Copy finished exports here when you want them easy to find.

The example final outputs live in:

`examples/masterchef-pink-cup/09-final-output/`

This folder intentionally keeps only lightweight example copies. The larger complete demo video is stored in the example project folder.
